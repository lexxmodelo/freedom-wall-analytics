# Secrets — NEVER commit
.env
.env.local
*.env

# Per-run artifacts
gpu_logs/*.log
gpu_logs/*.jsonl
api_cache/
checkpoints/
models/*.pkl
models/*.safetensors
outputs/*/

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/

# Editor
.vscode/
.idea/
*.swp

# Local venv + ad-hoc run logs (lead researcher's verification artifacts)
.venv/
venv/
bakeoff_run.log
test_umap_dim_fix.py
_test_embeddings_*.npy

# Per-researcher configs (each researcher creates their own from researcher_template.json)
configs/*.json
!configs/bertopic_config.json
!configs/gpu_config.json
!configs/researcher_template.json
